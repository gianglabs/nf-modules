nextflow_process {

    name "Test Process TABIX_INDEX_VCF"
    script "../main.nf"
    process "TABIX_INDEX_VCF"

    tag "modules"
    tag "modules_gianglabs"
    tag "bcftools"
    tag "bcftools/index"

    test("homo_sapiens - vcf") {

        when {
            process {
                """
                input[0] = Channel.of([
                    [ id: 'test' ],
                    file(params.modules_testdata_base_path + 'genomics/homo_sapiens/vcf/test_variants.vcf.gz', checkIfExists: true)
                ])
                """
            }
        }

        then {
            assertAll (
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }
    }
}
